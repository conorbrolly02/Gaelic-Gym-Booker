/**
 * PostCSS Configuration
 * 
 * Required for Tailwind CSS v4 processing.
 * Uses @tailwindcss/postcss package for the new Tailwind version.
 */
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
